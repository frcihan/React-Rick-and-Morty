import styled from "styled-components"

export const StyledCharacters = styled.div`
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
    margin: auto;
    padding: 3rem 10rem;
    background-repeat: no-repeat;
    background: radial-gradient(#2193b0, #006064, #111111);
    height: 100%;
    min-width: 930px;
`